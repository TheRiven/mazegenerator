use std::collections::HashSet;

pub fn solve(
    start: (u32, u32),
    end: (u32, u32),
    maze: &HashSet<(u32, u32)>,
) -> Option<Vec<&(u32, u32)>> {
    let result = Vec::new();

    Some(result)
}